{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Deploy",
            "type": "shell",
            "command": "rsync -av --exclude .vscode --exclude .webassets-cache --exclude __pycache__ . aws-twitter:~/dooh ",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}